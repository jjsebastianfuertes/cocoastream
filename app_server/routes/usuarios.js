/*var express = require('express');
var router = express.Router();
let ctrlUser = require('../controllers/usuarios')


router.get('/users', ctrlUser.usersList);
router.post('/user', ctrlUser.usersCreate);
router.get('/users/:userid', ctrlUser.usersReadOne);
router.put('/users/:userid', ctrlUser.usersUpdateOne);
router.delete('users/:userid', ctrlUser.usersDeleteOne);

module.exports = router;*/
